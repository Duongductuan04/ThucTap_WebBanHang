@model List<SimpleTaskApp.Statistics.Dto.LowStockProductDto>

<div class="card shadow">
  <div class="card-body">
    <h5 class="card-title">⚠️ Sản phẩm tồn kho thấp</h5>
    <table class="table table-bordered text-center">
      <thead class="table-danger">
        <tr><th>#</th><th>Sản phẩm</th><th>Tồn kho</th></tr>
      </thead>
      <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
          <tr>
            <td>@(i + 1)</td>
            <td>@Model[i].ProductName</td>
            <td>@Model[i].StockQuantity</td>
          </tr>
        }
      </tbody>
    </table>
  </div>
</div>
